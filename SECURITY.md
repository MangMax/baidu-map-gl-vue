# 安全政策

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| `3.x`（含 `3.0.0-beta.x`） | 支持，安全修复进入最新 minor / patch |
| `<= 2.x` | 不再维护（v2 源码已从本仓库移除，无法在此分支修复） |

仍在使用 2.x 的话，请参考[从 v2 迁移](https://MangMax.github.io/baidu-map-gl-vue/zh-CN/guide/migration-from-v2)。

## 上报漏洞

**请使用 GitHub 的私密报告表单**，不要开公开 issue、也不要在 Discussions 里贴细节：

<https://github.com/MangMax/baidu-map-gl-vue/security/advisories/new>

该表单只有维护者可见，我们会在这里与你来回沟通，并在修复后按你的意愿决定是否公开致谢。

如果能提供下面这些信息，会快很多：

- 受影响的版本范围
- 最小复现（仓库、StackBlitz 链接或代码片段）
- 影响面判断：能做什么、需要什么前置条件
- 若已有 PoC，请说明

## 响应预期

这是一个个人维护的开源项目，我们**尽力**做到：

- 3 个工作日内确认收到
- 7 天内给出初步评估（是否受理、影响的版本、大致修复方向）
- 修复在下一个 patch 或 minor 中发布，并在 GitHub Security Advisory 里披露

超过上述时间没有回应，欢迎在 issue 里发一条不带漏洞细节的催办。

## 范围

属于本仓库范围的：

- 组件库自身代码的问题（例如把不可信输入注入 DOM / 模板、`innerHTML` 类用法、事件处理中的注入）
- 构建产物与包出口的问题（例如类型泄漏、ESM/CJS 条件导出导致的加载异常）
- 由本仓库直接引入的依赖漏洞

不属于本仓库范围，请找对应上游：

- 百度地图 JSAPI 自身的行为与安全问题
- Vue 本身的问题

另外，本仓库不在 npm `postinstall` 里执行任何脚本，也不收集任何用户数据；如果你的环境观察到相反的行为，
请按安全问题上报。

## 致谢

我们不会对出于善意的安全研究采取法律行动。修复发布后，如果你愿意，我们会在 Security Advisory
与 CHANGELOG 中注明你的贡献。
